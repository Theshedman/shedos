name: build-packages

on:
  push:
    branches: [main]
    paths:
      - 'packaging/**'
      - 'packages/**'
      - 'scripts/render-meta-depends.sh'
      - 'scripts/build-aur-packages.sh'
      - 'scripts/build-shedos-packages.sh'
      - 'scripts/bump-version.sh'
      - 'scripts/compute-pkg-hash.sh'
      - 'scripts/refresh-local-hashes.sh'
      - '.github/workflows/build-packages.yml'
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: repo-publish
  cancel-in-progress: false

# contents: write lets the auto-bump step push back to main.
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    # Scopes the secrets.* lookups; without it they resolve to empty strings.
    environment: SHEDOS_ENV
    container:
      image: archlinux:latest
      # --privileged is required: build-shedos-packages.sh bind-mounts the local
      # repo to /srv for makepkg's file:// lookups, and mount needs CAP_SYS_ADMIN.
      # (Dropping it would mean eliminating that bind-mount first.)
      options: --privileged
      # Mount only the toolchain trees we trim below, not / (ownership warning).
      volumes:
        - /usr:/host/usr
        - /opt:/host/opt

    env:
      R2_ENDPOINT: https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com
      R2_BUCKET: shedos-repo

    steps:
      - name: Free host disk space
        run: |
          df -h /__w || true
          rm -rf \
              /host/usr/share/dotnet \
              /host/usr/local/lib/android \
              /host/opt/ghc \
              /host/opt/hostedtoolcache \
              /host/usr/local/share/boost \
              /host/usr/local/share/powershell \
              /host/usr/share/swift \
              /host/opt/microsoft \
              /host/usr/lib/jvm \
              2>/dev/null || true
          df -h /__w || true

      - name: Install build dependencies
        run: |
          pacman -Syu --noconfirm
          # shedos-* build with --nodeps, so install makedeps (scdoc) directly.
          pacman -S --needed --noconfirm base-devel git sudo gnupg rclone pacman-contrib scdoc python
          # archlinux:latest ships an empty keyring; populate it for --syncdeps.
          pacman-key --init
          pacman-key --populate archlinux
          # makepkg refuses to run as root.
          useradd -m -G wheel builder
          echo '%wheel ALL=(ALL) NOPASSWD: ALL' > /etc/sudoers.d/wheel
          chmod 440 /etc/sudoers.d/wheel

      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # Full history for the auto-bump push.
          fetch-depth: 0

      - name: Trust workspace for git
        # Container uid differs from checkout owner; avoids "dubious ownership".
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      # Resolve + render before the auto-bump so the regenerated shedos-meta
      # PKGBUILD participates in the content hash.
      - name: Resolve shedos-meta Arch closure
        run: bash scripts/resolve-meta-closure.sh

      - name: Refresh shedos-meta depends
        run: bash scripts/render-meta-depends.sh

      - name: Auto-bump release-hashes (main + manual)
        # Actor gate, not [skip ci]: [skip ci] would also block the tag-push
        # workflows on the bot's commit that `make release` needs.
        if: github.ref == 'refs/heads/main' && github.actor != 'github-actions[bot]'
        env:
          GIT_AUTHOR_NAME: shedos-ci[bot]
          GIT_AUTHOR_EMAIL: shedos-ci[bot]@users.noreply.github.com
          GIT_COMMITTER_NAME: shedos-ci[bot]
          GIT_COMMITTER_EMAIL: shedos-ci[bot]@users.noreply.github.com
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global user.name  "$GIT_AUTHOR_NAME"
          git config --global user.email "$GIT_AUTHOR_EMAIL"
          bash scripts/bump-version.sh
          if git diff --quiet -- packaging/ VERSION; then
              echo "Manifest in sync; no bump needed."
              exit 0
          fi
          git add packaging/ VERSION
          git commit -m "chore(release): auto-bump pkgrel for changed packages"
          git pull --rebase --autostash origin main
          git push origin HEAD:main

      - name: Validate release-hashes manifest (tag pushes)
        # CI doesn't rewrite tagged commits; the manifest must already match.
        if: startsWith(github.ref, 'refs/tags/')
        run: bash scripts/bump-version.sh --check

      - name: Wire makepkg config
        # System-wide drop-in: builds run as builduser, not builder, so a
        # per-user ~/.makepkg.conf would never be read.
        run: |
          install -d /etc/makepkg.conf.d
          cat > /etc/makepkg.conf.d/99-shedos-ci.conf <<'EOF'
          BUILDENV=(!distcc color !check !sign)
          # !debug so makepkg doesn't build -debug pkgs we'd sign and publish.
          OPTIONS=(strip docs !libtool !staticlibs emptydirs zipman purge !debug lto)
          MAKEFLAGS="-j$(nproc)"
          EOF

      - name: Hand the tree to the build user
        run: chown -R builder:builder .

      - name: Import signing key
        env:
          SIGNING_KEY: ${{ secrets.SHEDOS_REPO_SIGNING_KEY }}
        run: |
          mkdir -p /home/builder/.gnupg
          chmod 700 /home/builder/.gnupg
          chown -R builder:builder /home/builder/.gnupg

          if [[ -z "$SIGNING_KEY" ]]; then
              echo "FATAL: SHEDOS_REPO_SIGNING_KEY secret is empty or unset." >&2
              exit 1
          fi
          echo "signing key secret length: ${#SIGNING_KEY} bytes"
          first_line=$(printf '%s' "$SIGNING_KEY" | head -n1)
          echo "first line: $first_line"
          if [[ "$first_line" != "-----BEGIN PGP PRIVATE KEY BLOCK-----" ]]; then
              echo "FATAL: secret does not start with the armored PGP private key header." >&2
              echo "       Expected: -----BEGIN PGP PRIVATE KEY BLOCK-----" >&2
              echo "       Got:      $first_line" >&2
              echo "       Re-run key-ceremony.sh and paste shedos-private.asc verbatim." >&2
              exit 1
          fi

          printf '%s\n' "$SIGNING_KEY" | sudo -u builder gpg --batch --import

          FP=$(sudo -u builder gpg --list-secret-keys --with-colons \
               | awk -F: '/^fpr:/ { print $10; exit }')
          echo "GPG_FP=$FP" >> "$GITHUB_ENV"

          # Signing key must be a committed trusted fingerprint (any of them,
          # so a rotation window can sign with the new key).
          if ! grep -qx "$FP" \
              <(awk 'NF && $1 !~ /^#/' packaging/shedos-keyring/tree/shedos-trusted); then
              echo "FATAL: signing key fingerprint ($FP) is not listed in"
              echo "       committed shedos-trusted. Refusing to publish."
              exit 1
          fi

      - name: Restore AUR package cache
        # Separate restore/save, not the combined cache action: its post-save
        # is gated on success(), so a downstream failure would discard a good
        # prebuild. Save runs below with if: always().
        id: aur-cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: archiso/shedos-repo
          key: aur-${{ runner.os }}-${{ hashFiles('packages/aur.txt') }}
          restore-keys: |
            aur-${{ runner.os }}-

      - name: Pre-build AUR packages
        env:
          # Fail, don't warn-and-skip, when a declared PGP key can't be fetched.
          SHEDOS_AUR_STRICT_PGP: '1'
        run: scripts/build-aur-packages.sh

      - name: Save AUR package cache
        # Save regardless of downstream failures so the AUR prebuild survives.
        if: always() && steps.aur-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: archiso/shedos-repo
          key: aur-${{ runner.os }}-${{ hashFiles('packages/aur.txt') }}

      - name: Restore shedos-pkgs cache
        # github.sha key always-misses; restore-keys falls back to the latest.
        id: shedos-pkgs-cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: /tmp/shedos-pkg-cache
          key: shedos-pkgs-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            shedos-pkgs-${{ runner.os }}-

      - name: Stage cached shedos packages into shedos-repo
        run: |
          if [[ -d /tmp/shedos-pkg-cache ]]; then
              mkdir -p archiso/shedos-repo
              cp -v /tmp/shedos-pkg-cache/*.pkg.tar.zst archiso/shedos-repo/ 2>/dev/null || true
              cp -v /tmp/shedos-pkg-cache/*.pkg.tar.zst.sig archiso/shedos-repo/ 2>/dev/null || true
              chown -R builder:builder archiso/shedos-repo 2>/dev/null || true
              echo "Staged cached shedos packages from /tmp/shedos-pkg-cache."
          else
              echo "shedos-pkgs cache empty; build-shedos-packages.sh will build from source."
          fi

      - name: Build shedos-* packages
        run: scripts/build-shedos-packages.sh

      - name: Sign + stage packages for publication
        run: |
          install -d -o builder -g builder dist/x86_64
          chown -R builder:builder archiso/shedos-repo

          # aur-norepublish.txt: proprietary EULAs forbid republishing under
          # our signature. installer-only.txt: must not reach the public repo.
          mapfile -t norepublish < <(
              grep -hEv '^\s*(#|$)' \
                  packages/aur-norepublish.txt \
                  packages/installer-only.txt
          )

          # A cache-restored -debug pkg from before the !debug flag may persist.
          rm -f archiso/shedos-repo/*-debug-*.pkg.tar.zst \
                archiso/shedos-repo/*-debug-*.pkg.tar.zst.sig

          staged=0 reused=0 skipped=0
          for pkg in archiso/shedos-repo/*.pkg.tar.zst; do
              [[ -f $pkg ]] || continue
              base=$(basename "$pkg")
              pkgname=${base%-*-*-*.pkg.tar.zst}
              if [[ $pkgname == *-debug ]]; then
                  echo "skip  $pkgname (debug)"
                  skipped=$((skipped+1))
                  continue
              fi
              skip=0
              for n in "${norepublish[@]}"; do
                  if [[ $pkgname == "$n" ]]; then skip=1; break; fi
              done
              if [[ $skip -eq 1 ]]; then
                  echo "skip  $pkgname (norepublish)"
                  skipped=$((skipped+1))
                  continue
              fi
              # Reuse a cached sig; sigs don't expire. On key rotation, purge
              # the GHA cache manually.
              if [[ -f "${pkg}.sig" ]]; then
                  echo "reuse $pkgname (cached signature)"
                  cp "$pkg" "${pkg}.sig" dist/x86_64/
                  reused=$((reused+1))
                  continue
              fi
              echo "sign  $pkgname"
              sudo -u builder gpg --batch --detach-sign --no-armor \
                  --local-user "$GPG_FP" \
                  --output "$pkg.sig" "$pkg"
              # cp not mv: keep the package in the cache snapshot.
              cp "$pkg" "$pkg.sig" dist/x86_64/
              staged=$((staged+1))
          done
          echo "staged $staged freshly signed; $reused cache-restored sigs; $skipped proprietary"
          ls -la dist/x86_64/ | head -30

      - name: Stage cached repo DB into dist/x86_64/
        # Pacman fetches <repo>.db / <repo>.files, not the .tar.gz form, so
        # materialize those names from the archive copies.
        run: |
          install -d -o builder -g builder dist/x86_64
          for f in shedos.db.tar.gz shedos.db.tar.gz.sig \
                   shedos.files.tar.gz shedos.files.tar.gz.sig; do
              if [[ -f /tmp/shedos-pkg-cache/$f ]]; then
                  cp -v /tmp/shedos-pkg-cache/$f dist/x86_64/
              fi
          done
          if [[ -f dist/x86_64/shedos.db.tar.gz ]]; then
              cp -v dist/x86_64/shedos.db.tar.gz    dist/x86_64/shedos.db
          fi
          if [[ -f dist/x86_64/shedos.db.tar.gz.sig ]]; then
              cp -v dist/x86_64/shedos.db.tar.gz.sig    dist/x86_64/shedos.db.sig
          fi
          if [[ -f dist/x86_64/shedos.files.tar.gz ]]; then
              cp -v dist/x86_64/shedos.files.tar.gz dist/x86_64/shedos.files
          fi
          if [[ -f dist/x86_64/shedos.files.tar.gz.sig ]]; then
              cp -v dist/x86_64/shedos.files.tar.gz.sig dist/x86_64/shedos.files.sig
          fi

      - name: Build repo DB
        run: |
          # Reuse the cached DB only when nothing rebuilt AND its package
          # count matches disk; the count check catches deletions.
          disk_count=$(ls -1 dist/x86_64/*.pkg.tar.zst 2>/dev/null | wc -l)
          db_count=0
          if [[ -f dist/x86_64/shedos.db.tar.gz ]]; then
              db_count=$(bsdtar -tf dist/x86_64/shedos.db.tar.gz 2>/dev/null \
                         | awk -F/ '$2 == "desc" {print $1}' | sort -u | wc -l)
          fi
          if [[ ! -s /tmp/built-pkgs.txt ]] \
             && [[ -f dist/x86_64/shedos.db.tar.gz ]] \
             && [[ "$disk_count" -eq "$db_count" ]]; then
              echo "No packages rebuilt and DB ($db_count) matches disk ($disk_count) — reusing cached repo DB"
              ls -la dist/x86_64/shedos.db.tar.gz dist/x86_64/shedos.files.tar.gz
              exit 0
          fi
          echo "Rebuilding repo DB: built=$([[ -s /tmp/built-pkgs.txt ]] && wc -l </tmp/built-pkgs.txt || echo 0), disk=$disk_count, db=$db_count"
          cd dist/x86_64
          # repo-add only adds; wipe the cached DB so removed pkgs drop out.
          rm -f shedos.db shedos.db.sig shedos.db.tar.gz shedos.db.tar.gz.sig \
                shedos.files shedos.files.sig shedos.files.tar.gz shedos.files.tar.gz.sig
          sudo -u builder repo-add --sign --key "$GPG_FP" \
              shedos.db.tar.gz *.pkg.tar.zst
          # repo-add leaves these as symlinks; R2 has no symlinks, so
          # materialize real files. rm first or cp sees src == dst.
          for name in shedos.db shedos.db.sig shedos.files shedos.files.sig; do
              [[ -L $name ]] && rm -f "$name"
          done
          cp shedos.db.tar.gz shedos.db
          cp shedos.db.tar.gz.sig shedos.db.sig
          cp shedos.files.tar.gz shedos.files
          cp shedos.files.tar.gz.sig shedos.files.sig

      - name: Stage shedos packages for cache save
        id: shedos-pkgs-stage
        if: always()
        run: |
          mkdir -p /tmp/shedos-pkg-cache
          shopt -s nullglob
          declare -A keep_pkgs=()
          for dir in packaging/*/; do
              [[ -f "$dir/PKGBUILD" ]] || continue
              name=$(basename "$dir")
              keep_pkgs[$name]=1
          done
          for f in /tmp/shedos-pkg-cache/*.pkg.tar.zst; do
              base=$(basename "$f")
              pkgname=${base%-*-*-*.pkg.tar.zst}
              parent=${pkgname%-debug}
              if [[ -z ${keep_pkgs[$parent]:-} ]]; then
                  echo "  cache prune phantom: $base"
                  rm -f "$f" "${f}.sig"
              fi
          done
          for dir in packaging/*/; do
              pkgname=$(basename "$dir")
              [[ -f "$dir/PKGBUILD" ]] || continue
              pkgver=$(awk -F= '/^pkgver=/ {print $2; exit}' "$dir/PKGBUILD")
              pkgrel=$(awk -F= '/^pkgrel=/ {print $2; exit}' "$dir/PKGBUILD")
              for stale in /tmp/shedos-pkg-cache/${pkgname}-*.pkg.tar.zst; do
                  base=$(basename "$stale")
                  [[ "$base" == "${pkgname}-${pkgver}-${pkgrel}-"*.pkg.tar.zst ]] && continue
                  [[ "$base" == "${pkgname}-debug-${pkgver}-${pkgrel}-"*.pkg.tar.zst ]] && continue
                  echo "  cache prune stale-version: $base"
                  rm -f "$stale" "${stale}.sig"
              done
          done
          has_files=0
          for f in archiso/shedos-repo/*.pkg.tar.zst; do
              base=$(basename "$f")
              pkgname=${base%-*-*-*.pkg.tar.zst}
              parent=${pkgname%-debug}
              [[ -n ${keep_pkgs[$parent]:-} ]] || continue
              cp -v "$f" /tmp/shedos-pkg-cache/
              [[ -f "${f}.sig" ]] && cp -v "${f}.sig" /tmp/shedos-pkg-cache/
              has_files=1
          done
          for f in dist/x86_64/shedos.db.tar.gz dist/x86_64/shedos.db.tar.gz.sig \
                   dist/x86_64/shedos.files.tar.gz dist/x86_64/shedos.files.tar.gz.sig; do
              [[ -f $f ]] && cp -v "$f" /tmp/shedos-pkg-cache/
          done
          ls -la /tmp/shedos-pkg-cache/ | head -20
          echo "has_files=$has_files" >> "$GITHUB_OUTPUT"

      - name: Save shedos-pkgs cache
        # has_files gate: an empty cache would shadow healthy prior caches.
        if: always() && steps.shedos-pkgs-stage.outputs.has_files == '1'
        uses: actions/cache/save@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: /tmp/shedos-pkg-cache
          key: shedos-pkgs-${{ runner.os }}-${{ github.sha }}

      - name: Publish to R2
        env:
          RCLONE_CONFIG_R2_TYPE: s3
          RCLONE_CONFIG_R2_PROVIDER: Cloudflare
          RCLONE_CONFIG_R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          RCLONE_CONFIG_R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          RCLONE_CONFIG_R2_ENDPOINT: ${{ env.R2_ENDPOINT }}
          RCLONE_CONFIG_R2_REGION: auto
          # Bucket-scoped R2 tokens lack CreateBucket; without this, rclone's
          # bucket probe 403s on copyto to the bucket root.
          RCLONE_S3_NO_CHECK_BUCKET: "true"
        run: |
          # Channel routing: push/RC tag → /test; RC tag also snapshots to
          # /rc/<tag>; stable tag promotes the latest RC snapshot (not live
          # /test, which the soak's main-pushes overwrite) via copy so older
          # versions survive for downgrade.
          if [[ "$GITHUB_REF_TYPE" == "tag" ]] && [[ "$GITHUB_REF_NAME" != *-rc* ]]; then
              mapfile -t snaps < <(rclone lsf --dirs-only "r2:$R2_BUCKET/rc/" 2>/dev/null \
                  | grep -E "^${GITHUB_REF_NAME}-rc[0-9]+/$" | sort -V)
              if (( ${#snaps[@]} == 0 )); then
                  echo "::error::No RC snapshot under /rc/ matches $GITHUB_REF_NAME." \
                       "Cut and soak an RC (make release-rc) before tagging stable."
                  exit 1
              fi
              snap=${snaps[-1]%/}
              echo "Stable tag $GITHUB_REF_NAME: promoting /rc/$snap/x86_64/ → /stable/x86_64/"
              rclone copy "r2:$R2_BUCKET/rc/$snap/x86_64/" "r2:$R2_BUCKET/stable/x86_64/" --progress
          else
              echo "Push or RC tag: publishing dist/x86_64/ → /test/x86_64/"
              rclone sync dist/x86_64/ "r2:$R2_BUCKET/test/x86_64/" --progress
              # Mirror the DB as [shedostest] on /test so a stable box can opt
              # into the canary. Out of dist/ to keep /rc and /stable clean.
              for ext in db db.sig files files.sig; do
                  [[ -f "dist/x86_64/shedos.$ext" ]] \
                      && rclone copyto "dist/x86_64/shedos.$ext" \
                          "r2:$R2_BUCKET/test/x86_64/shedostest.$ext"
              done
              if [[ "$GITHUB_REF_TYPE" == "tag" ]]; then
                  echo "RC tag $GITHUB_REF_NAME: freezing snapshot at /rc/$GITHUB_REF_NAME/x86_64/"
                  rclone copy dist/x86_64/ "r2:$R2_BUCKET/rc/$GITHUB_REF_NAME/x86_64/" --progress
              fi
          fi
          # Bootstrap pubkey at the bucket root; migration fetches it before
          # the keyring is installed.
          rclone copyto packaging/shedos-keyring/tree/shedos.gpg \
              "r2:$R2_BUCKET/shedos.gpg"
