#!/bin/sh
# Stub: `pacman -Qq shedos-system` fails (not installed) — legacy
# state. Other invocations succeed silently.
printf '%s: pacman %s\n' "$(date -u +%T)" "$*" >>"${PROBE_LOG:-/dev/null}"
case "$1 $2" in
    "-Qq shedos-system") exit 1 ;;
esac
exit 0
