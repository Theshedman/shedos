---
title: Getting started
description: Download, flash, install, and land on a configured desktop.
order: 10
section: Guide
---
import DownloadSteps from "~/components/DownloadSteps.astro";
import { STABLE_ISO_NAME, STABLE_ISO_URL, STABLE_SHA256_URL } from "~/generated/version";

ShedOS ships as a single bootable ISO. Flash it to a USB stick, run the
installer, and reboot into a configured Hyprland desktop with your
toolchain already installed.

## Make the install USB

<DownloadSteps
    isoName={STABLE_ISO_NAME}
    isoUrl={STABLE_ISO_URL}
    sha256Url={STABLE_SHA256_URL}
/>

## Run the installer

Boot the USB from your firmware's boot menu. The installer walks you
through partitioning, user creation, and locale, with ShedOS defaults
pre-seeded. Your Git identity (name and email) is collected on the user
page, alongside the account you create.

## First boot

The machine boots to the ShedOS greeter — log in with the account you
created (there is no autologin on installed systems). The desktop —
Hyprland, waybar, mako, kitty — comes up already themed and ready.

On your first login a short welcome tour overlays the desktop and walks
you through the essentials. `Esc` skips it, and `shedman tour` replays it
any time.

## Next

- [Upgrading](/docs/upgrading) — how updates and dotfile merges work.
- [Commands](/docs/commands) — the full `shedman` CLI reference.
- [Keybindings](/docs/keybindings) — every shortcut the desktop ships with.
