---
interface Props {
  variant?: "note" | "warn";
  label?: string;
}
const { variant, label } = Astro.props;
---
<div class:list={["callout", variant]}>
  {label && <span class="lbl">{label}</span>}
  <slot />
</div>
